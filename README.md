
  # TransGlobal

  This is a code bundle for TransGlobal. The original project is available at https://www.figma.com/design/557zaZ0gdk8uqyIUnV4RMU/TransGlobal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  